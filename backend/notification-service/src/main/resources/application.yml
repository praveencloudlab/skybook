server:
  port: 8085

spring:
  application:
    name: notification-service

  kafka:
    bootstrap-servers: localhost:9092

    consumer:
      group-id: notification-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer

      properties:
        spring.json.trusted.packages: "com.skybook.praveen.common.*"
        spring.json.value.default.type: "com.skybook.praveen.common.event.EmailEvent"
  mail:
    host: smtp.gmail.com
    port: 587
    username: pravs.sr@gmail.com
    password: cdip vbxn mtse fkme
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


skybook:
  kafka:
    topics:
      email-events: skybook.email.events