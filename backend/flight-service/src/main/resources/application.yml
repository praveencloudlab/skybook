server:
  port: 8082

spring:
  application:
    name: flight-service

  datasource:
    url: jdbc:postgresql://localhost:5432/skybook_auth
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true