# API gateway origin (FRONTEND_MODULE.md §8).
# Unset defaults to http://localhost:8080, which is what `docker compose up`
# exposes. Point this at a deployed gateway (or a k8s ingress) to run the same
# build elsewhere - no code change needed.
VITE_API_BASE_URL=http://localhost:8080
